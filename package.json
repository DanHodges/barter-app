{
  "name": "barter-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "aws-amplify": "^0.2.4",
    "aws-amplify-react": "^0.1.32",
    "dotenv": "^5.0.0",
    "dotenv-webpack": "^1.5.4",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prettier": "node_modules/.bin/prettier 'src/*.{js,json}' --write",
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^6.1.0",
    "prettier": "1.10.2"
  },
  "lint-staged": {
    "*.{js,json,css,md}": ["prettier --write", "git add"]
  },
  "prettier": {
    "trailingComma": "all"
  }
}
